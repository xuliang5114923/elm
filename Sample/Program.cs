using System;
using ELMAPI;
using System.Collections.Generic;

namespace Sample
{
    class Program
    {
        static void Main(string[] args)
        {
            #region 获取评论
            //UGC uGC = new UGC();
            //string result = uGC.getOrderRatesByShopId("300594461", System.DateTime.Today.AddDays(-60).ToString("s"), System.DateTime.Today.ToString("s"), 0, 10);
            #endregion

            #region 店铺服务
            //ShopService shopService = new ShopService();
            //shopService.getShop(300594461);

            
            //ELMAPI.Model.ShopProperties shopProperties = new ELMAPI.Model.ShopProperties();
            //shopProperties.name = "店看看测试";
            //shopProperties.addressText = "上海市徐汇区南京路888号";
            //shopService.updateShop(300594461,shopProperties);
            #endregion
        }
    }
}
